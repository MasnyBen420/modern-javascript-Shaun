const calcArea = function(radius){
    let area= Math.round(Math.PI,2) * radius**2;
    return(area);
}


const area = calcArea(5);
console.log(area);

const calcVol = function(area){

};
calcVol(area)